const userObj = {
    name: 'Dejene',
    age: 25,
    role: 'student',
    classes: ['html and css', 'javascript 1'],
    hobbies: {
        favourite: 'coding',
        sports: 'biking to class'
    }
}
const userArr = [userObj, 2, 'hello'];
    let sumAge = 0;
for(let i =0; i<userArr.length; i++) {


console.log(userArr[i.age])

sumAge == userArr[i].age;
}
 const avgAge= sumAge / userArr.length
 console.log(avgAge)  