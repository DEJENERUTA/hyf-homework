class Order {
    constructor(order, type, status, created, modified){ 
        this.order = order,
        this.type = type,
         this.status=status,
         this.created  = new Date(Date.now());
         this.modified = new Date(Date.now());

    }
   }




   module.express = order;